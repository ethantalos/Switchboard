# Switchboard

- Keep the codebase lean. Add structure only when the work needs it.
- Disk is ground truth; verify claims against current files, not session memory.
- Read evidence before proposing a cause or fix; label uncertainty.
- Use ARCHITECTURE.md to orient in unfamiliar areas. Keep it current-state only.
- Update it in the same change when adding, moving, or renaming files, engines, config keys, or data flows.
- Keep useful project context in README.md and current tasks in TODO.md.
- Preserve the human's and other agents' edits.
- Keep private project data, credentials, and local sessions out of Git.
- Run relevant checks; do not claim untested integrations work.
- Commit or push only when explicitly asked.
- Write concise, plain responses.
- Use gstack /browse for web browsing; never use mcp__claude-in-chrome__*.
